[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "openenv-bug-triage"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
    "pyyaml>=6.0",
    "openai>=1.0",
    "openenv-core>=0.2.0",
    "gradio>=5.0",
    "rich>=13.0",
]

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.hatch.build.targets.wheel]
packages = ["env", "tasks", "agents"]
